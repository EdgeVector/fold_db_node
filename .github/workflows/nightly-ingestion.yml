name: Nightly Ingestion Test

on:
  schedule:
    # 4am UTC = 9pm PT (previous day)
    - cron: '0 4 * * *'
  workflow_dispatch:
    inputs:
      provider:
        description: 'AI Provider (Anthropic or Ollama)'
        required: false
        default: 'Anthropic'

concurrency:
  group: nightly-ingestion
  cancel-in-progress: true

jobs:
  ingestion-test:
    name: Sample Data Ingestion
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      RUSTFLAGS: -C linker=clang -C link-arg=-fuse-ld=mold
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install mold linker
        run: sudo apt-get update && sudo apt-get install -y mold

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Configure git for private dependencies
        run: git config --global url."https://x-access-token:${{ secrets.PRIVATE_DEPS_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Stub frontend dist for RustEmbed
        run: mkdir -p src/server/static-react/dist && echo '<html></html>' > src/server/static-react/dist/index.html

      - name: Build binaries
        run: cargo build --bin folddb_server --bin schema_service

      - name: Run ingestion test
        id: ingestion
        run: |
          ./scripts/test-sample-ingestion.sh \
            --provider "${{ github.event.inputs.provider || 'Anthropic' }}" \
            --max-wait 30 2>&1 | tee /tmp/ingestion-output.txt

      - name: Save test output
        if: failure()
        run: |
          {
            echo "## Ingestion Test Output"
            echo '```'
            cat /tmp/ingestion-output.txt
            echo '```'
            echo ""
            echo "## Server Log (last 100 lines)"
            echo '```'
            tail -100 /tmp/tmp.*/server.log 2>/dev/null || echo "No server log found"
            echo '```'
            echo ""
            echo "## Schema Service Log (last 50 lines)"
            echo '```'
            tail -50 /tmp/tmp.*/schema_service.log 2>/dev/null || echo "No schema service log found"
            echo '```'
          } > /tmp/failure-context.txt

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ingestion-test-logs
          path: |
            /tmp/ingestion-output.txt
            /tmp/tmp.*/server.log
            /tmp/tmp.*/schema_service.log
          retention-days: 7

  auto-fix:
    name: Claude Code Auto-Fix
    needs: ingestion-test
    if: failure()
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download failure logs
        uses: actions/download-artifact@v4
        with:
          name: ingestion-test-logs
          path: /tmp/failure-logs

      - name: Prepare failure context
        id: context
        run: |
          {
            echo "The nightly sample data ingestion test failed."
            echo ""
            echo "## Test Output"
            echo '```'
            cat /tmp/failure-logs/ingestion-output.txt 2>/dev/null || echo "No test output"
            echo '```'
            echo ""
            echo "## Server Log (last 80 lines)"
            echo '```'
            find /tmp/failure-logs -name "server.log" -exec tail -80 {} \; 2>/dev/null || echo "No server log"
            echo '```'
            echo ""
            echo "## Schema Service Log (last 40 lines)"
            echo '```'
            find /tmp/failure-logs -name "schema_service.log" -exec tail -40 {} \; 2>/dev/null || echo "No schema log"
            echo '```'
          } > /tmp/claude-prompt-context.txt

      - name: Run Claude Code to fix
        uses: anthropics/claude-code-action@beta
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            The nightly ingestion integration test failed. This test runs
            `scripts/test-sample-ingestion.sh` which starts a FoldDB node,
            smart-folder-ingests all files in `sample_data/`, and asserts
            100% success.

            Read the failure context below, diagnose the root cause, fix it,
            and create a PR. The fix should be in the Rust code (fold_db_node),
            not in the test script itself (unless the script has a bug).

            Key files:
            - scripts/test-sample-ingestion.sh (the test)
            - src/ingestion/ (ingestion pipeline)
            - src/ingestion/smart_folder/ (smart folder scan + ingest)
            - src/ingestion/ai/ (AI backend, prompt, response parsing)
            - src/ingestion/mutation_generator.rs (field mapping)
            - src/fold_node/schema_client.rs (schema service client)

            Failure context:
            $(cat /tmp/claude-prompt-context.txt)
          allowed_tools: "Bash,Read,Write,Edit,Glob,Grep"
          timeout_minutes: 25
